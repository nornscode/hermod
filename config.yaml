github:
  user: amackera
  webhook_secret: ${HERMOD_GITHUB_WEBHOOK_SECRET}
  token: ${HERMOD_GITHUB_TOKEN}
  repos:
    - nornscode/norns
    - nornscode/mimir
    # - amackera/<personal-repo>

slack:
  bot_token: ${HERMOD_SLACK_BOT_TOKEN}
  signing_secret: ${HERMOD_SLACK_SIGNING_SECRET}
  user_id: U01ABCDEFGH  # Anson's Slack user ID

norns:
  runtime_url: http://localhost:4000
  api_key: ${HERMOD_NORNS_API_KEY}

rules:
  author_staleness_hours: 48
  reviewer_staleness_hours: 24
